<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wiuh" >

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WiUH" >
        <activity
            android:name=".activities.NotificationHistoryActivity"
            android:exported="false" />
        <activity
            android:name=".activities.PersonalSetupActivity"
            android:exported="false" />
        <activity android:name=".activities.SetupActivity" />
        <activity android:name=".activities.BoardActivity" />
        <activity
            android:name=".ui.memo.AddMemoActivity"
            android:parentActivityName=".activities.BoardActivity" />
        <activity
            android:name=".ui.community.AddPostActivity"
            android:parentActivityName=".activities.BoardActivity" />
        <activity
            android:name=".ui.memo.MemoModify"
            android:parentActivityName=".activities.BoardActivity" />
        <activity
            android:name=".ui.community.PostModify"
            android:parentActivityName=".activities.BoardActivity" />
        <activity
            android:name=".ui.community.PostDetailActivity"
            android:parentActivityName=".activities.BoardActivity" />
        <activity
            android:name=".ui.memo.MemoDetailActivity"
            android:parentActivityName=".activities.BoardActivity" />
        <activity
            android:name=".activities.SignUpActivity"
            android:parentActivityName=".activities.LoginActivity" />
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>